[test]
preload = ["./tests/setup.ts"]
coverage = true
coverageReporter = ["text", "lcov"]
coverageExclude = [
  "tests/**",
  "**/*.test.ts",
  "**/*.spec.ts",
  "node_modules/**",
  "dist/**"
]
coverageThreshold = {
  lines = 80,
  functions = 80,
  branches = 70
}
